# Copia este archivo como ".env" y completa los valores reales.
# El archivo .env NO se sube a git (está ignorado), por eso hay que crearlo localmente.

# Servidor
PORT=3000

# Base de datos (Aiven MySQL/MariaDB)
DB_HOST=mysql-agrotraz-elpoli-proyect.j.aivencloud.com
DB_PORT=21976
DB_USER=avnadmin
DB_PASSWORD=TU_CONTRASENA_DEL_AVEN
DB_NAME=dbagrotraz
DB_SSL=true

# JWT
JWT_SECRET=TU_CLAVE_SECRETA_FIRMA_TOKENS
JWT_EXPIRES_IN=8h